{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "CUDA Debug",
      "type": "cuda-gdb", // Typ debuggera
      "request": "launch",
      "program": "${workspaceFolder}/NeuralNetwork", // Plik wykonywalny w głównym katalogu
      "args": [],
      "stopAtEntry": false,
      "cwd": "${workspaceFolder}",
      "environment": [],
      "preLaunchTask": "build" // Zadanie kompilacji przed debugowaniem
    }
  ]
}